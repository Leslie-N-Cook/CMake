## Makefiles to CMake

To convert to CMake run the following commands from the [make](../make) directory of the project.

### Commands 

- `mkdir my_build`
- `cd my_build`
- `cmake ..`
- `cmake --build .`